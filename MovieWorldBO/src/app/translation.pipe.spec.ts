import { Translation} from './translation.pipe';

describe('TranslationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new Translation();
    expect(pipe).toBeTruthy();
  });
});
